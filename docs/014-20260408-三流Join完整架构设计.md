# FlinkADSArbitrageJob 三流 Join 完整架构设计

## 文档信息
- **文档编号**: 014
- **日期**: 2026-04-08
- **功能模块**: 套利作业 - 三流 Join 完整架构
- **状态**: 设计中

## 🎯 业务需求

### 完整流程
```
1. 现货流 + 合约流 → 双流 Join → 计算套利机会
2. 套利机会流 + 订单流 → 三流 Join → 判断交易决策
3. 交易决策 → 触发订单 → WebSocket 发送
4. 订单确认 → 状态管理 → 输出明细到 Doris
```

### 核心功能
1. **套利机会计算**: 实时计算现货和合约价差
2. **订单流监控**: 订阅 WebSocket 订单更新
3. **交易决策**: 根据套利机会和当前持仓决定开仓/平仓/保持
4. **订单触发**: 通过 WebSocket 发送交易订单
5. **状态管理**: 维护每个币对的持仓状态
6. **明细输出**: 输出所有交易明细到 Doris

## 📊 数据流架构

### 完整数据流图
```
┌─────────────────────────────────────────────────────────────────────┐
│                         Flink ADS Arbitrage Job                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────┐                                                    │
│  │ Kafka Spot   │──┐                                                 │
│  │ (现货价格)    │  │                                                 │
│  └──────────────┘  │                                                 │
│                    ├──→ Interval Join ──→ 套利机会计算               │
│  ┌──────────────┐  │         ↓                                       │
│  │ Kafka Swap   │──┘         │                                       │
│  │ (合约价格)    │            │                                       │
│  └──────────────┘            │                                       │
│                              ↓                                       │
│                    ┌──────────────────┐                              │
│                    │ 套利机会流        │                              │
│                    │ ArbitrageOpp     │                              │
│                    └──────────────────┘                              │
│                              │                                       │
│                              │ keyBy(symbol)                         │
│                              ↓                                       │
│  ┌──────────────┐   ┌──────────────────┐                            │
│  │ WebSocket    │──→│  CoProcessFunc   │                            │
│  │ 订单流        │   │  交易决策引擎     │                            │
│  └──────────────┘   └──────────────────┘                            │
│                              │                                       │
│                              ├──→ 开仓决策 ──→ 下单                   │
│                              ├──→ 平仓决策 ──→ 下单                   │
│                              └──→ 保持状态                            │
│                              │                                       │
│                              ↓                                       │
│                    ┌──────────────────┐                              │
│                    │  状态管理         │                              │
│                    │  PositionState   │                              │
│                    └──────────────────┘                              │
│                              │                                       │
│                              ↓                                       │
│                    ┌──────────────────┐                              │
│                    │  交易明细         │                              │
│                    │  TradeRecord     │                              │
│                    └──────────────────┘                              │
│                              │                                       │
│                              ↓                                       │
│                    ┌──────────────────┐                              │
│                    │  Doris Sink      │                              │
│                    │  交易明细表       │                              │
│                    └──────────────────┘                              │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔧 核心组件设计

### 1. 套利机会流 (ArbitrageOpportunity)

**数据结构**:
```java
public static class ArbitrageOpportunity {
    public String symbol;           // 交易对
    public BigDecimal spotPrice;    // 现货价格
    public BigDecimal swapPrice;    // 合约价格
    public BigDecimal spread;       // 价差
    public BigDecimal spreadRate;   // 价差率(%)
    public String direction;        // 套利方向
    public long timestamp;          // 时间戳
}
```

**来源**: 现货流 + 合约流 Interval Join

### 2. 订单流 (OrderUpdate)

**数据结构**:
```java
public static class OrderUpdate {
    public String orderId;          // 订单ID
    public String symbol;           // 交易对
    public String instType;         // 产品类型(SPOT/SWAP)
    public String side;             // 买卖方向(buy/sell)
    public String state;            // 订单状态(filled/canceled)
    public BigDecimal fillPrice;    // 成交价格
    public BigDecimal fillSize;     // 成交数量
    public long timestamp;          // 时间戳
}
```

**来源**: OKXOrderWebSocketSource

### 3. 交易决策引擎 (TradingDecisionProcessor)

**功能**:
- 接收套利机会和订单更新
- 维护持仓状态
- 判断开仓/平仓/保持
- 触发交易订单

**状态管理**:
```java
// 持仓状态
ValueState<PositionState> positionState;

// 待确认订单
MapState<String, PendingOrder> pendingOrders;
```

**决策逻辑**:
```
IF 收到套利机会:
    IF 无持仓 AND 价差率 > 开仓阈值:
        → 下单开仓(现货 + 合约)
        → 保存待确认订单
    ELSE IF 有持仓 AND (价差回归 OR 超时 OR 止损):
        → 下单平仓(现货 + 合约)
        → 保存待确认订单
    ELSE:
        → 保持状态不变

IF 收到订单更新:
    IF 订单成交:
        → 更新待确认订单状态
        → 检查是否所有订单都成交
        → 如果都成交,确认开仓/平仓
        → 更新持仓状态
        → 输出交易明细
```

### 4. 交易明细输出 (TradeRecord)

**数据结构**:
```java
public class TradeRecord {
    public String symbol;           // 交易对
    public String action;           // 动作(OPEN/CLOSE)
    public String direction;        // 方向(LONG_SPOT_SHORT_SWAP)
    public BigDecimal amount;       // 交易金额
    public BigDecimal spotPrice;    // 现货价格
    public BigDecimal swapPrice;    // 合约价格
    public BigDecimal spreadRate;   // 价差率
    public BigDecimal profit;       // 盈亏
    public String closeReason;      // 平仓原因
    public long holdTimeMs;         // 持仓时间
    public long timestamp;          // 时间戳
}
```

**输出目标**: Doris 表 `dwd_arbitrage_trades`

## 💻 代码实现

### 主流程代码框架

```java
// ========== 步骤 1: 创建现货流 ==========
DataStream<SpotPrice> spotStream = ...;

// ========== 步骤 2: 创建合约流 ==========
DataStream<SwapPrice> swapStream = ...;

// ========== 步骤 3: Interval Join 计算套利机会 ==========
DataStream<ArbitrageOpportunity> arbitrageStream = spotStream
    .keyBy(spot -> spot.symbol)
    .intervalJoin(swapStream.keyBy(swap -> swap.symbol))
    .between(Time.seconds(-10), Time.seconds(10))
    .process(new ArbitrageCalculator())
    .name("Calculate Arbitrage");

// ========== 步骤 4: 创建订单流 ==========
DataStream<OrderUpdate> orderStream = env
    .addSource(new OKXOrderWebSocketSource(config))
    .map(new OrderUpdateParser())
    .name("Parse Order Update");

// ========== 步骤 5: Connect 套利机会流和订单流 ==========
DataStream<TradeRecord> tradeStream = arbitrageStream
    .keyBy(opp -> opp.symbol)
    .connect(orderStream.keyBy(order -> order.symbol))
    .process(new TradingDecisionProcessor(config))
    .name("Trading Decision");

// ========== 步骤 6: 输出到 Doris ==========
tradeStream
    .map(new TradeRecordToJson())
    .sinkTo(dorisSink)
    .name("Doris Trade Sink");
```

### 交易决策处理器

```java
public static class TradingDecisionProcessor 
        extends KeyedCoProcessFunction<String, ArbitrageOpportunity, OrderUpdate, TradeRecord> {
    
    private final ConfigLoader config;
    private final OKXTradingService tradingService;
    
    // 持仓状态
    private ValueState<PositionState> positionState;
    
    // 待确认订单
    private MapState<String, PendingOrder> pendingOrders;
    
    @Override
    public void open(Configuration parameters) {
        // 初始化状态
        positionState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("position", PositionState.class));
        
        pendingOrders = getRuntimeContext().getMapState(
            new MapStateDescriptor<>("pending-orders", String.class, PendingOrder.class));
        
        // 初始化交易服务
        tradingService = new OKXTradingService(config);
    }
    
    @Override
    public void processElement1(
            ArbitrageOpportunity opportunity,
            Context ctx,
            Collector<TradeRecord> out) throws Exception {
        
        PositionState position = positionState.value();
        
        // 决策 1: 开仓
        if (position == null || !position.isOpen()) {
            if (shouldOpen(opportunity)) {
                openPosition(opportunity, out);
            }
        }
        // 决策 2: 平仓
        else {
            if (shouldClose(opportunity, position)) {
                closePosition(opportunity, position, out);
            }
        }
    }
    
    @Override
    public void processElement2(
            OrderUpdate order,
            Context ctx,
            Collector<TradeRecord> out) throws Exception {
        
        // 处理订单更新
        if ("filled".equals(order.state)) {
            handleOrderFilled(order, out);
        }
    }
    
    private boolean shouldOpen(ArbitrageOpportunity opp) {
        BigDecimal threshold = new BigDecimal("0.005"); // 0.5%
        return opp.spreadRate.abs().compareTo(threshold) > 0;
    }
    
    private boolean shouldClose(ArbitrageOpportunity opp, PositionState pos) {
        // 条件 1: 价差回归
        BigDecimal closeThreshold = new BigDecimal("0.002"); // 0.2%
        boolean spreadConverged = opp.spreadRate.abs().compareTo(closeThreshold) <= 0;
        
        // 条件 2: 超时
        long holdTime = System.currentTimeMillis() - pos.getOpenTime();
        boolean timeout = holdTime > 3600000; // 1小时
        
        // 条件 3: 止损
        BigDecimal unrealizedPnL = calculateUnrealizedPnL(opp, pos);
        boolean stopLoss = unrealizedPnL.compareTo(new BigDecimal("-10")) < 0;
        
        return spreadConverged || timeout || stopLoss;
    }
    
    private void openPosition(ArbitrageOpportunity opp, Collector<TradeRecord> out) 
            throws Exception {
        
        logger.info("🎯 开仓: {} | 价差率: {}%", opp.symbol, opp.spreadRate);
        
        // 下单
        String spotOrderId = tradingService.buySpot(opp.symbol, new BigDecimal("100"));
        String swapOrderId = tradingService.shortSwap(opp.symbol, new BigDecimal("100"), 1);
        
        // 保存待确认订单
        PendingOrder pending = new PendingOrder();
        pending.symbol = opp.symbol;
        pending.spotOrderId = spotOrderId;
        pending.swapOrderId = swapOrderId;
        pending.action = "OPEN";
        pending.createTime = System.currentTimeMillis();
        
        pendingOrders.put(spotOrderId, pending);
        pendingOrders.put(swapOrderId, pending);
    }
    
    private void closePosition(
            ArbitrageOpportunity opp, 
            PositionState pos, 
            Collector<TradeRecord> out) throws Exception {
        
        logger.info("🔄 平仓: {} | 价差率: {}%", opp.symbol, opp.spreadRate);
        
        // 下单
        String spotOrderId = tradingService.sellSpot(opp.symbol, pos.getAmount());
        String swapOrderId = tradingService.closeShortSwap(opp.symbol, pos.getAmount());
        
        // 保存待确认订单
        PendingOrder pending = new PendingOrder();
        pending.symbol = opp.symbol;
        pending.spotOrderId = spotOrderId;
        pending.swapOrderId = swapOrderId;
        pending.action = "CLOSE";
        pending.createTime = System.currentTimeMillis();
        
        pendingOrders.put(spotOrderId, pending);
        pendingOrders.put(swapOrderId, pending);
    }
    
    private void handleOrderFilled(OrderUpdate order, Collector<TradeRecord> out) 
            throws Exception {
        
        PendingOrder pending = pendingOrders.get(order.orderId);
        if (pending == null) {
            return; // 不是我们的订单
        }
        
        // 更新订单状态
        if (order.orderId.equals(pending.spotOrderId)) {
            pending.spotFilled = true;
            pending.spotFillPrice = order.fillPrice;
        } else if (order.orderId.equals(pending.swapOrderId)) {
            pending.swapFilled = true;
            pending.swapFillPrice = order.fillPrice;
        }
        
        // 检查是否都成交
        if (pending.spotFilled && pending.swapFilled) {
            if ("OPEN".equals(pending.action)) {
                confirmOpen(pending, out);
            } else {
                confirmClose(pending, out);
            }
            
            // 清理待确认订单
            pendingOrders.remove(pending.spotOrderId);
            pendingOrders.remove(pending.swapOrderId);
        }
    }
    
    private void confirmOpen(PendingOrder pending, Collector<TradeRecord> out) 
            throws Exception {
        
        logger.info("✅ 开仓确认: {}", pending.symbol);
        
        // 更新持仓状态
        PositionState position = new PositionState();
        position.setSymbol(pending.symbol);
        position.setOpen(true);
        position.setDirection("LONG_SPOT_SHORT_SWAP");
        position.setAmount(new BigDecimal("100"));
        position.setEntrySpotPrice(pending.spotFillPrice);
        position.setEntrySwapPrice(pending.swapFillPrice);
        position.setOpenTime(System.currentTimeMillis());
        
        positionState.update(position);
        
        // 输出交易明细
        TradeRecord record = new TradeRecord();
        record.symbol = pending.symbol;
        record.action = "OPEN";
        record.direction = "LONG_SPOT_SHORT_SWAP";
        record.amount = new BigDecimal("100");
        record.spotPrice = pending.spotFillPrice;
        record.swapPrice = pending.swapFillPrice;
        record.timestamp = System.currentTimeMillis();
        
        out.collect(record);
    }
    
    private void confirmClose(PendingOrder pending, Collector<TradeRecord> out) 
            throws Exception {
        
        logger.info("✅ 平仓确认: {}", pending.symbol);
        
        PositionState position = positionState.value();
        
        // 计算盈亏
        BigDecimal entrySpread = position.getEntrySwapPrice().subtract(position.getEntrySpotPrice());
        BigDecimal exitSpread = pending.swapFillPrice.subtract(pending.spotFillPrice);
        BigDecimal profit = exitSpread.subtract(entrySpread);
        
        // 更新持仓状态
        position.setOpen(false);
        positionState.update(position);
        
        // 输出交易明细
        TradeRecord record = new TradeRecord();
        record.symbol = pending.symbol;
        record.action = "CLOSE";
        record.direction = position.getDirection();
        record.amount = position.getAmount();
        record.spotPrice = pending.spotFillPrice;
        record.swapPrice = pending.swapFillPrice;
        record.profit = profit;
        record.holdTimeMs = System.currentTimeMillis() - position.getOpenTime();
        record.timestamp = System.currentTimeMillis();
        
        out.collect(record);
    }
    
    private BigDecimal calculateUnrealizedPnL(ArbitrageOpportunity opp, PositionState pos) {
        BigDecimal entrySpread = pos.getEntrySwapPrice().subtract(pos.getEntrySpotPrice());
        BigDecimal currentSpread = opp.swapPrice.subtract(opp.spotPrice);
        return currentSpread.subtract(entrySpread);
    }
}
```

## 📋 数据库表设计

### Doris 表: dwd_arbitrage_trades

```sql
CREATE TABLE IF NOT EXISTS crypto_dw.dwd_arbitrage_trades (
    symbol VARCHAR(50) COMMENT '交易对',
    action VARCHAR(20) COMMENT '动作(OPEN/CLOSE)',
    direction VARCHAR(50) COMMENT '方向',
    amount DECIMAL(20, 8) COMMENT '交易金额',
    spot_price DECIMAL(20, 8) COMMENT '现货价格',
    swap_price DECIMAL(20, 8) COMMENT '合约价格',
    spread_rate DECIMAL(10, 6) COMMENT '价差率(%)',
    profit DECIMAL(20, 8) COMMENT '盈亏',
    close_reason VARCHAR(50) COMMENT '平仓原因',
    hold_time_ms BIGINT COMMENT '持仓时间(毫秒)',
    timestamp BIGINT COMMENT '时间戳'
)
DUPLICATE KEY(symbol, timestamp)
DISTRIBUTED BY HASH(symbol) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);
```

### Doris 表: ads_position_status

```sql
CREATE TABLE IF NOT EXISTS crypto_dw.ads_position_status (
    symbol VARCHAR(50) COMMENT '交易对',
    is_open BOOLEAN COMMENT '是否持仓',
    direction VARCHAR(50) COMMENT '方向',
    amount DECIMAL(20, 8) COMMENT '持仓金额',
    entry_spot_price DECIMAL(20, 8) COMMENT '开仓现货价格',
    entry_swap_price DECIMAL(20, 8) COMMENT '开仓合约价格',
    open_time BIGINT COMMENT '开仓时间',
    update_time BIGINT COMMENT '更新时间'
)
UNIQUE KEY(symbol)
DISTRIBUTED BY HASH(symbol) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);
```

## 🎯 下一步实现

1. ✅ 修改 `FlinkADSArbitrageJob.java` 添加订单流
2. ✅ 实现 `TradingDecisionProcessor` 交易决策引擎
3. ✅ 实现 `OrderUpdateParser` 订单解析器
4. ✅ 创建 Doris 表
5. ✅ 测试完整流程

---

**文档时间**: 2026-04-08  
**作者**: Kiro AI Assistant  
**状态**: 设计完成,待实现
