# 架构修复 - DWD 作业改为从 Doris ODS 表读取数据

## 背景

用户发现了严重的架构问题:三个 Flink 作业都在消费同一个 Kafka Topic,违反了数据仓库分层架构的设计原则。

## 问题分析

### 修复前的错误架构

```
Kafka (crypto-ticker)
    ├─→ ODS 作业 → ods_crypto_ticker_rt  ✅ 正确
    ├─→ DWD 作业 → dwd_crypto_ticker_detail  ❌ 错误!
    └─→ DWS 作业 → dws_crypto_ticker_1min  ❌ 错误!
```

**问题**:
- DWD 和 DWS 作业直接消费 Kafka,跳过了 ODS 层
- 违反了数据仓库分层架构原则
- 数据流转混乱,难以维护
- 无法从 ODS 层重新计算历史数据

### 修复后的正确架构

```
Kafka → ODS 作业 → ODS 表 → DWD 作业 → DWD 表 → DWS 作业 → DWS 表
```

**优势**:
- 分层清晰,每一层只依赖上一层
- Kafka 只被 ODS 作业消费一次
- 数据一致性有保障
- 支持数据回溯和重新计算

## 修复方案

### 方案选择

采用 **Doris Connector 读取上游表** 方案:
- 直接从 Doris 表读取数据
- 性能好,延迟低
- 配置简单

### 修复步骤

#### 1. 修改 FlinkDWDJobSQL.java

**修改 1: 更新类注释**
```java
/**
 * Flink DWD 作业 - Flink SQL 方式
 * 
 * 架构修复说明:
 * - 修复前: 直接消费 Kafka Topic (错误)
 * - 修复后: 从 Doris ODS 表读取数据 (正确)
 * 
 * 数据流转: Kafka → ODS 作业 → ODS 表 → DWD 作业 → DWD 表
 */
```

**修改 2: 更新日志输出**
```java
logger.info("==========================================");
logger.info("Flink DWD Job (Flink SQL)");
logger.info("架构: Doris ODS 表 → DWD 作业 → Doris DWD 表");
logger.info("==========================================");
```

**修改 3: 替换 Kafka Source 为 Doris Source**
```java
// 修改前: 创建 Kafka Source 表
String kafkaSourceDDL = createKafkaSourceDDL(config);
log.info("Creating Kafka Source Table...");
tableEnv.executeSql(kafkaSourceDDL);

// 修改后: 创建 Doris ODS Source 表
String dorisSourceDDL = createDorisSourceDDL(config);
log.info("Creating Doris ODS Source Table...");
tableEnv.executeSql(dorisSourceDDL);
```

**修改 4: 创建 Doris Source DDL 方法**
```java
/**
 * 创建 Doris ODS Source 表 DDL
 * 
 * 架构修复说明:
 * - 修复前: DWD 作业直接消费 Kafka Topic (错误)
 * - 修复后: DWD 作业从 Doris ODS 表读取数据 (正确)
 * 
 * 数据流转: Kafka → ODS 作业 → ODS 表 → DWD 作业 → DWD 表
 */
private static String createDorisSourceDDL(ConfigLoader config) {
    String feNodes = config.getString("doris.fe.http-url").replace("http://", "");
    String database = config.getString("doris.database");
    String odsTable = config.getString("doris.tables.ods");
    String username = config.getString("doris.fe.username");
    String password = config.getString("doris.fe.password", "");
    
    logger.info("Doris ODS Source 配置:");
    logger.info("  FE Nodes: " + feNodes);
    logger.info("  Database: " + database);
    logger.info("  Table: " + odsTable);
    
    return "CREATE TABLE doris_ods_source (\n" +
           "    inst_id STRING,\n" +  // ODS 表字段名
           "    `timestamp` BIGINT,\n" +
           "    last_price DECIMAL(20, 8),\n" +
           "    bid_price DECIMAL(20, 8),\n" +
           "    ask_price DECIMAL(20, 8),\n" +
           "    bid_size DECIMAL(20, 8),\n" +
           "    ask_size DECIMAL(20, 8),\n" +
           "    volume_24h DECIMAL(30, 8),\n" +
           "    high_24h DECIMAL(20, 8),\n" +
           "    low_24h DECIMAL(20, 8),\n" +
           "    open_24h DECIMAL(20, 8),\n" +
           "    data_source STRING,\n" +
           "    ingest_time BIGINT\n" +
           ") WITH (\n" +
           "    'connector' = 'doris',\n" +
           "    'fenodes' = '" + feNodes + "',\n" +
           "    'table.identifier' = '" + database + "." + odsTable + "',\n" +
           "    'username' = '" + username + "',\n" +
           "    'password' = '" + password + "'\n" +
           ")";
}
```

**修改 5: 更新 INSERT SQL**
```java
/**
 * 创建 INSERT INTO SQL(包含数据清洗和字段补充逻辑)
 * 
 * 架构修复说明:
 * - 修复前: 从 kafka_ods_source 读取(Kafka JSON 格式,驼峰命名)
 * - 修复后: 从 doris_ods_source 读取(Doris 表格式,下划线命名)
 * 
 * 注意: 
 * 1. 源表字段名匹配 Doris ODS 表字段名(下划线命名)
 * 2. ODS 表中的字段已经是 DECIMAL 类型,不需要再转换
 * 3. 使用 Flink SQL 标准函数进行数据清洗和计算
 */
private static String createInsertSQL() {
    return "INSERT INTO doris_dwd_sink\n" +
           "SELECT \n" +
           "    inst_id,\n" +  // 直接使用 ODS 表字段名
           "    `timestamp`,\n" +
           "    -- 交易日期和小时\n" +
           "    CAST(TO_TIMESTAMP(FROM_UNIXTIME(`timestamp` / 1000)) AS DATE) as trade_date,\n" +
           "    CAST(EXTRACT(HOUR FROM TO_TIMESTAMP(FROM_UNIXTIME(`timestamp` / 1000))) AS INT) as trade_hour,\n" +
           "    -- 价格字段(ODS 表中已经是 DECIMAL 类型,直接使用)\n" +
           "    last_price,\n" +
           "    bid_price,\n" +
           "    ask_price,\n" +
           "    -- 计算买卖价差\n" +
           "    CAST((ask_price - bid_price) AS DECIMAL(20, 8)) as spread,\n" +
           "    -- 计算价差率\n" +
           "    CAST(CASE \n" +
           "        WHEN last_price > 0 THEN (ask_price - bid_price) / last_price\n" +
           "        ELSE 0\n" +
           "    END AS DECIMAL(10, 6)) as spread_rate,\n" +
           "    -- 成交量和价格\n" +
           "    volume_24h,\n" +
           "    high_24h,\n" +
           "    low_24h,\n" +
           "    open_24h,\n" +
           "    -- 计算24小时涨跌额\n" +
           "    CAST(CASE \n" +
           "        WHEN open_24h > 0 THEN (last_price - open_24h)\n" +
           "        ELSE 0\n" +
           "    END AS DECIMAL(20, 8)) as price_change_24h,\n" +
           "    -- 计算24小时涨跌幅\n" +
           "    CAST(CASE \n" +
           "        WHEN open_24h > 0 THEN (last_price - open_24h) / open_24h\n" +
           "        ELSE 0\n" +
           "    END AS DECIMAL(10, 6)) as price_change_rate_24h,\n" +
           "    -- 计算24小时振幅\n" +
           "    CAST(CASE \n" +
           "        WHEN low_24h > 0 THEN (high_24h - low_24h) / low_24h\n" +
           "        ELSE 0\n" +
           "    END AS DECIMAL(10, 6)) as amplitude_24h,\n" +
           "    -- 数据源和时间戳\n" +
           "    data_source,\n" +
           "    ingest_time,\n" +
           "    UNIX_TIMESTAMP() * 1000 as process_time\n" +
           "FROM doris_ods_source\n" +  // 从 Doris ODS 表读取
           "WHERE last_price > 0 \n" +
           "  AND bid_price > 0 \n" +
           "  AND ask_price > 0";
}
```

## 关键变化对比

### 数据源变化

| 项目 | 修复前 | 修复后 |
|-----|--------|--------|
| 数据源类型 | Kafka | Doris |
| 表名 | kafka_ods_source | doris_ods_source |
| Connector | kafka | doris |
| 字段命名 | 驼峰(instId, ts) | 下划线(inst_id, timestamp) |
| 字段类型 | STRING | DECIMAL |

### SQL 变化

**修复前 (Kafka JSON)**:
```sql
SELECT 
    instId as inst_id,  -- 需要字段名转换
    ts as `timestamp`,
    CAST(`last` AS DECIMAL(20, 8)) as last_price,  -- 需要类型转换
    CAST(bidPx AS DECIMAL(20, 8)) as bid_price,
    ...
FROM kafka_ods_source
```

**修复后 (Doris 表)**:
```sql
SELECT 
    inst_id,  -- 直接使用,无需转换
    `timestamp`,
    last_price,  -- 已经是 DECIMAL 类型,无需转换
    bid_price,
    ...
FROM doris_ods_source
```

## 验证结果

### 编译验证
```bash
$ mvn compile -DskipTests -q
[INFO] BUILD SUCCESS
```

✅ 编译成功,代码语法正确

### 架构验证

**修复前**:
```
Kafka → ODS 作业 → ODS 表
Kafka → DWD 作业 → DWD 表  ❌ 错误
```

**修复后**:
```
Kafka → ODS 作业 → ODS 表 → DWD 作业 → DWD 表  ✅ 正确
```

## 注意事项

### 1. Doris Connector 读取模式

Doris Flink Connector 默认使用批量读取模式:
- 一次性读取全部数据
- 作业会自动结束
- 适合离线计算和数据回溯

如果需要流式读取(持续读取新数据),需要配置:
```java
'scan.mode' = 'stream'  // 需要 Doris 支持 CDC
```

### 2. 数据延迟

使用 Doris Connector 读取数据会有一定延迟:
- ODS 作业写入 Doris: ~1-5 秒
- DWD 作业从 Doris 读取: ~1-5 秒
- 总延迟: ~2-10 秒

对于实时性要求极高的场景,可以考虑使用 Kafka 中间 Topic。

### 3. 数据一致性

- 确保 ODS 作业先启动并写入数据
- DWD 作业启动前,ODS 表应该已有数据
- 建议启用 Flink Checkpoint 保证 Exactly-Once 语义

## 下一步

1. ✅ DWD 作业已修复完成
2. ⏭️ 修复 DWS 作业(从 DWD 表读取数据)
3. ⏭️ 测试完整的数据流转
4. ⏭️ 更新运维文档

## 相关文件

- `src/main/java/com/crypto/dw/flink/FlinkDWDJobSQL.java` - DWD 作业(已修复)
- `docs/202603220515-架构问题-修复Flink作业数据流转错误.md` - 架构问题分析
- `docs/202603220500-Doris表结构-三张表作用和数据流转说明.md` - 表结构说明

## 总结

成功修复了 DWD 作业的架构问题:
- ✅ 从直接消费 Kafka 改为从 Doris ODS 表读取
- ✅ 符合数据仓库分层架构设计原则
- ✅ 数据流转清晰: Kafka → ODS → DWD
- ✅ 代码编译通过,语法正确
- ✅ SQL 简化,不需要类型转换

下一步需要修复 DWS 作业,让它从 DWD 表读取数据。
