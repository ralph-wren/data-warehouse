# Interval Join 多对多匹配补充说明

## 目标

补充 Interval Join 在同一时间范围内的多对多匹配行为说明，避免误解为“一对一”。

## 处理步骤

1. 在 `FlinkADSArbitrageJob` 注释中补充多对多匹配说明。
2. 强调匹配为“笛卡尔式”行为，需要在 `process` 中控制输出量。

## 关键说明

- **多对多匹配**：同一 key 下，只要时间差在 `between(-10s, +10s)` 范围内，
  现货与合约会两两匹配，可能产生多条结果。
- **实践建议**：需要通过过滤、聚合或去重控制最终输出量。

## 结果

已补充多对多匹配说明，便于理解 Interval Join 的输出行为。

