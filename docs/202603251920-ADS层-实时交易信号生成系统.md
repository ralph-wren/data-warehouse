# ADS层 - 实时交易信号生成系统

## 概述

创建时间：2026-03-25 19:20  
作业名称：FlinkADSTradingSignalJob  
业务场景：基于多维度市场数据实时生成交易信号，帮助交易员捕捉市场机会

## 业务价值

这是一个真实的生产级交易信号系统，具有以下特点：

1. **实时性**：毫秒级响应市场变化，捕捉瞬息万变的交易机会
2. **智能化**：综合价格、成交量、波动率等多维度指标，智能生成交易建议
3. **风险控制**：自动计算风险评分，分离高风险信号，保护资金安全
4. **策略灵活**：支持保守、中等、激进三种策略，适应不同风险偏好
5. **可扩展**：使用广播状态动态更新策略，无需重启作业

## Flink高级特性应用

### 1. CEP (Complex Event Processing) - 复杂事件处理

**应用场景**：检测价格突破模式

```java
Pattern<MarketData, ?> breakoutPattern = Pattern
    .<MarketData>begin("start")
    .where(new SimpleCondition<MarketData>() {
        @Override
        public boolean filter(MarketData data) {
            // 价格在相对稳定区间
            return data.priceChangePercent.abs().compareTo(new BigDecimal("0.5")) < 0;
        }
    })
    .times(3).consecutive()  // 连续3次稳定
    .followedBy("breakout")
    .where(new SimpleCondition<MarketData>() {
        @Override
        public boolean filter(MarketData data) {
            // 突然大幅波动
            return data.priceChangePercent.abs().compareTo(new BigDecimal("1.5")) > 0;
        }
    })
    .within(Time.minutes(5));  // 5分钟内发生
```

**业务含义**：
- 检测"盘整-突破"模式：价格连续3次稳定（波动<0.5%），然后突然大幅波动（>1.5%）
- 这是技术分析中的经典突破形态，通常预示着趋势的开始
- 5分钟时间窗口确保模式的时效性

### 2. Broadcast State - 广播状态

**应用场景**：动态更新交易策略参数

```java
// 创建广播流
BroadcastStream<TradingStrategy> strategyBroadcast = strategyStream
    .broadcast(STRATEGY_STATE_DESC);

// 关联数据流和广播流
enrichedSignals = breakoutSignals
    .keyBy(signal -> signal.symbol)
    .connect(strategyBroadcast)
    .process(new SignalEnrichmentFunction());
```

**业务价值**：
- 策略参数存储在Redis中，可以随时修改
- Flink作业每30秒从Redis读取最新策略，通过广播流分发到所有并行实例
- 无需重启作业即可调整风险阈值、成交量阈值等参数
- 支持为不同交易对配置不同策略（BTC保守、山寨币激进）

### 3. Keyed State - 键控状态

**应用场景**：维护每个交易对的历史统计

```java
// 价格历史状态 - 用于计算波动率
private transient ValueState<List<BigDecimal>> priceHistoryState;

// 信号统计状态 - 用于计算置信度
private transient MapState<String, Integer> signalStatsState;
```

**业务价值**：
- 为每个交易对独立维护价格历史（最近100个价格点）
- 基于历史数据计算波动率（标准差），评估市场风险
- 统计各类信号的历史表现，动态调整置信度
- 状态数据持久化到RocksDB，支持故障恢复

### 4. Side Output - 侧输出流

**应用场景**：分离高风险信号

```java
// 定义侧输出标签
private static final OutputTag<String> HIGH_RISK_SIGNAL_TAG = 
    new OutputTag<String>("high-risk-signal"){};

// 输出到侧输出流
if (riskScore.compareTo(strategy.riskThreshold) > 0) {
    context.output(HIGH_RISK_SIGNAL_TAG, tradingSignal.toJson());
}

// 获取侧输出流
DataStream<String> highRiskSignals = tradingSignals
    .getSideOutput(HIGH_RISK_SIGNAL_TAG);
```

**业务价值**：
- 主流输出所有交易信号
- 侧输出流单独输出高风险信号（风险评分>阈值）
- 高风险信号写入独立的Doris表，便于监控和告警
- 避免高风险信号淹没在正常信号中

### 5. Sliding Window - 滑动窗口

**应用场景**：计算多时间维度指标

```java
enrichedSignals
    .keyBy(signal -> signal.symbol)
    .window(SlidingEventTimeWindows.of(
        Duration.ofMinutes(15),  // 窗口大小15分钟
        Duration.ofMinutes(5)    // 滑动步长5分钟
    ))
    .process(new TradingSignalGenerator());
```

**业务价值**：
- 15分钟窗口：捕捉中短期趋势
- 5分钟滑动：每5分钟更新一次信号，保持实时性
- 窗口内聚合多个突破信号，提高信号可靠性
- 计算窗口内的平均价格、总成交量等统计指标

### 6. Watermark - 水位线

**应用场景**：处理乱序数据

```java
WatermarkStrategy
    .<String>forBoundedOutOfOrderness(Duration.ofSeconds(5))
    .withTimestampAssigner((event, timestamp) -> {
        // 从事件中提取时间戳
        JsonNode node = mapper.readTree(event);
        return node.get("ts").asLong();
    })
```

**业务价值**：
- 允许5秒的数据乱序
- 基于事件时间（Event Time）而非处理时间（Processing Time）
- 确保窗口计算的准确性，即使数据延迟到达
- 适应网络波动和Kafka分区间的时间差异

### 7. Rich Function - 富函数

**应用场景**：访问外部系统（Redis）

```java
public static class TradingStrategySource extends RichMapFunction<Long, TradingStrategy> 
        implements SourceFunction<TradingStrategy> {
    
    private transient RedisConnectionManager redisManager;
    
    @Override
    public void open(Configuration parameters) {
        redisManager = new RedisConnectionManager(config);
    }
    
    @Override
    public void run(SourceContext<TradingStrategy> ctx) throws Exception {
        // 从Redis读取策略配置
        try (Jedis jedis = redisManager.getConnection()) {
            Set<String> keys = jedis.keys("trading:strategy:*");
            // ...
        }
    }
}
```

**业务价值**：
- 在open()方法中初始化Redis连接，复用连接池
- 定期从Redis读取最新策略配置
- 支持热更新，无需重启Flink作业
- 统一的连接管理，避免连接泄漏

## 数据流架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         Kafka Topic                              │
│                    crypto_ticker_spot                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MarketDataParser                              │
│              解析JSON -> MarketData对象                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CEP Pattern Matching                          │
│              检测"盘整-突破"价格模式                             │
│         (连续3次稳定 -> 突然大幅波动)                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Broadcast State Join                            │
│         关联突破信号 + 交易策略(从Redis读取)                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              Sliding Window Aggregation                          │
│         15分钟窗口，5分钟滑动，计算多维度指标                    │
│    (平均价格、总成交量、波动率、置信度、风险评分)                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Trading Signal Generator                        │
│         生成交易信号(BUY/SELL/HOLD)                              │
│         计算目标价格、止损价格                                   │
└────────────┬───────────────────────────┬────────────────────────┘
             │                           │
             ▼                           ▼
┌────────────────────────┐  ┌───────────────────────────────────┐
│   Main Output Stream   │  │   Side Output Stream              │
│   所有交易信号         │  │   高风险信号(风险评分>阈值)       │
└────────────┬───────────┘  └───────────┬───────────────────────┘
             │                           │
             ▼                           ▼
┌────────────────────────┐  ┌───────────────────────────────────┐
│        Doris           │  │        Doris                      │
│ ads_trading_signals    │  │ ads_high_risk_signals             │
└────────────────────────┘  └───────────────────────────────────┘
```

## 核心算法

### 1. 波动率计算（标准差）

```java
private BigDecimal calculateVolatility(List<BigDecimal> prices) {
    // 计算平均值
    BigDecimal mean = prices.stream()
        .reduce(BigDecimal.ZERO, BigDecimal::add)
        .divide(new BigDecimal(prices.size()), 8, RoundingMode.HALF_UP);
    
    // 计算方差
    BigDecimal variance = prices.stream()
        .map(p -> p.subtract(mean).pow(2))
        .reduce(BigDecimal.ZERO, BigDecimal::add)
        .divide(new BigDecimal(prices.size()), 8, RoundingMode.HALF_UP);
    
    // 返回标准差
    return new BigDecimal(Math.sqrt(variance.doubleValue()));
}
```

**业务含义**：
- 波动率反映价格的不稳定程度
- 高波动率 = 高风险 + 高收益潜力
- 用于计算止损距离和风险评分

### 2. 信号类型判断

```java
private String determineSignalType(
        BigDecimal priceChange,
        BigDecimal volatility,
        BigDecimal volume,
        TradingStrategy strategy) {
    
    // 价格上涨且成交量充足 -> BUY
    if (priceChange.compareTo(strategy.priceThreshold) > 0 &&
        volume.compareTo(strategy.volumeThreshold) > 0) {
        return "BUY";
    }
    
    // 价格下跌且成交量充足 -> SELL
    if (priceChange.compareTo(strategy.priceThreshold.negate()) < 0 &&
        volume.compareTo(strategy.volumeThreshold) > 0) {
        return "SELL";
    }
    
    // 其他情况 -> HOLD
    return "HOLD";
}
```

**业务逻辑**：
- BUY信号：价格上涨超过阈值 + 成交量放大（有资金推动）
- SELL信号：价格下跌超过阈值 + 成交量放大（有抛压）
- HOLD信号：价格波动小或成交量不足（观望）

### 3. 目标价格和止损计算

```java
private Tuple2<BigDecimal, BigDecimal> calculateTargetAndStopLoss(
        BigDecimal currentPrice,
        String signalType,
        BigDecimal volatility,
        TradingStrategy strategy) {
    
    // 根据策略类型调整风险收益比
    BigDecimal riskRewardRatio;
    switch (strategy.strategyType) {
        case "AGGRESSIVE":
            riskRewardRatio = new BigDecimal("3.0");  // 3:1
            break;
        case "CONSERVATIVE":
            riskRewardRatio = new BigDecimal("1.5");  // 1.5:1
            break;
        default:  // MODERATE
            riskRewardRatio = new BigDecimal("2.0");  // 2:1
    }
    
    // 使用波动率的2倍作为止损距离
    BigDecimal stopLossDistance = volatility.multiply(new BigDecimal("2"));
    
    if ("BUY".equals(signalType)) {
        stopLoss = currentPrice.subtract(stopLossDistance);
        targetPrice = currentPrice.add(stopLossDistance.multiply(riskRewardRatio));
    }
    // ...
}
```

**业务逻辑**：
- 止损距离 = 2倍波动率（适应市场波动）
- 目标价格 = 当前价格 + 止损距离 × 风险收益比
- 激进策略：3:1风险收益比（高收益高风险）
- 保守策略：1.5:1风险收益比（稳健）
- 中等策略：2:1风险收益比（平衡）

### 4. 置信度计算

```java
private BigDecimal calculateConfidence(
        int signalCount,
        BigDecimal volatility,
        BigDecimal volume,
        TradingStrategy strategy) {
    
    // 基础置信度：信号数量越多，置信度越高
    BigDecimal baseConfidence = new BigDecimal(Math.min(signalCount * 10, 50));
    
    // 成交量加成：成交量越大，置信度越高
    BigDecimal volumeBonus = volume.compareTo(strategy.volumeThreshold) > 0 ?
        new BigDecimal("20") : BigDecimal.ZERO;
    
    // 波动率惩罚：波动率越高，置信度越低
    BigDecimal volatilityPenalty = volatility.multiply(new BigDecimal("5"));
    
    BigDecimal confidence = baseConfidence
        .add(volumeBonus)
        .subtract(volatilityPenalty);
    
    // 限制在0-100范围内
    return confidence.setScale(2, RoundingMode.HALF_UP);
}
```

**业务逻辑**：
- 信号数量多 → 置信度高（多次确认）
- 成交量大 → 置信度高（有资金支持）
- 波动率高 → 置信度低（市场不稳定）

### 5. 风险评分计算

```java
private BigDecimal calculateRiskScore(
        BigDecimal volatility,
        BigDecimal priceChange,
        BigDecimal volume,
        TradingStrategy strategy) {
    
    // 波动率风险：波动率越高，风险越大
    BigDecimal volatilityRisk = volatility.multiply(new BigDecimal("10"));
    
    // 价格变化风险：价格变化越剧烈，风险越大
    BigDecimal priceRisk = priceChange.abs().multiply(new BigDecimal("5"));
    
    // 流动性风险：成交量越小，风险越大
    BigDecimal liquidityRisk = volume.compareTo(strategy.volumeThreshold) < 0 ?
        new BigDecimal("30") : BigDecimal.ZERO;
    
    BigDecimal riskScore = volatilityRisk
        .add(priceRisk)
        .add(liquidityRisk);
    
    // 限制在0-100范围内
    return riskScore.setScale(2, RoundingMode.HALF_UP);
}
```

**业务逻辑**：
- 波动率风险：市场不稳定
- 价格变化风险：价格剧烈波动
- 流动性风险：成交量不足，难以成交

## 部署步骤

### 1. 初始化Redis策略配置

```bash
# 设置交易策略到Redis
chmod +x scripts/init-trading-strategy.sh
./scripts/init-trading-strategy.sh
```

策略配置说明：
- BTC/ETH：保守策略，风险阈值30-35，适合大资金稳健操作
- BNB/SOL/XRP：中等策略，风险阈值50，平衡收益和风险
- DOGE/SHIB/PEPE：激进策略，风险阈值70-75，适合小资金博取高收益
- DeFi代币：中等策略，风险阈值45-50
- Layer2/AI概念币：激进策略，风险阈值65-70

### 2. 创建Doris表

```bash
# 连接Doris
mysql -h doris-fe -P 9030 -u root

# 执行建表SQL
source sql/doris/create_ads_trading_signal_table.sql
```

### 3. 编译打包

```bash
mvn clean package -DskipTests
```

### 4. 提交Flink作业

```bash
# 设置执行权限
chmod +x run-flink-ads-trading-signal.sh

# 提交作业
./run-flink-ads-trading-signal.sh
```

## 监控指标

### 1. 业务指标

```sql
-- 查看最新交易信号
SELECT 
    symbol,
    signal_type,
    current_price,
    target_price,
    stop_loss,
    confidence,
    risk_score,
    strategy_type,
    create_time
FROM crypto_ads.ads_trading_signals
ORDER BY timestamp DESC
LIMIT 20;

-- 统计各类信号数量
SELECT 
    signal_type,
    COUNT(*) as signal_count,
    AVG(confidence) as avg_confidence,
    AVG(risk_score) as avg_risk_score
FROM crypto_ads.ads_trading_signals
WHERE create_time >= DATE_SUB(NOW(), INTERVAL 1 HOUR)
GROUP BY signal_type;

-- 查看高风险信号
SELECT 
    symbol,
    signal_type,
    risk_score,
    volatility,
    create_time
FROM crypto_ads.ads_high_risk_signals
ORDER BY risk_score DESC
LIMIT 10;

-- 统计各交易对信号表现
SELECT 
    symbol,
    COUNT(*) as total_signals,
    SUM(CASE WHEN signal_type = 'BUY' THEN 1 ELSE 0 END) as buy_signals,
    SUM(CASE WHEN signal_type = 'SELL' THEN 1 ELSE 0 END) as sell_signals,
    AVG(confidence) as avg_confidence
FROM crypto_ads.ads_trading_signals
WHERE create_time >= DATE_SUB(NOW(), INTERVAL 24 HOUR)
GROUP BY symbol
ORDER BY total_signals DESC;
```

### 2. Flink作业指标

- Checkpoint成功率：应保持在95%以上
- 数据处理延迟：应在秒级
- 状态大小：监控RocksDB状态增长
- 背压情况：检查是否有算子背压

## 优化建议

### 1. 性能优化

- **并行度调整**：根据数据量调整并行度，建议4-8
- **状态清理**：定期清理过期的价格历史状态
- **窗口大小**：根据业务需求调整窗口大小和滑动步长
- **Checkpoint间隔**：生产环境建议1-3分钟

### 2. 算法优化

- **引入更多技术指标**：MACD、RSI、布林带等
- **机器学习模型**：使用历史数据训练信号预测模型
- **多时间周期**：同时分析1分钟、5分钟、15分钟多个周期
- **市场情绪分析**：结合社交媒体情绪数据

### 3. 风险控制

- **仓位管理**：根据风险评分动态调整建议仓位
- **止盈策略**：增加移动止盈逻辑
- **市场过滤**：在极端行情下暂停信号生成
- **回测验证**：使用历史数据回测信号准确率

## 学习要点

### 1. CEP模式匹配

- 理解begin、followedBy、within等API
- 掌握SimpleCondition和IterativeCondition的区别
- 学习如何定义复杂的事件序列模式

### 2. 广播状态

- 理解广播流的应用场景（配置、规则、维度表）
- 掌握BroadcastState的读写操作
- 学习如何实现动态配置更新

### 3. 键控状态

- 理解ValueState、ListState、MapState的使用场景
- 掌握状态的生命周期管理
- 学习状态的序列化和持久化

### 4. 侧输出流

- 理解侧输出流的应用场景（数据分流、异常处理）
- 掌握OutputTag的定义和使用
- 学习如何处理多个侧输出流

### 5. 窗口计算

- 理解滑动窗口、滚动窗口、会话窗口的区别
- 掌握ProcessWindowFunction的使用
- 学习如何在窗口中访问状态

## 总结

这个实时交易信号生成系统是一个综合性的Flink应用，展示了：

1. **CEP**：检测价格突破模式
2. **Broadcast State**：动态更新策略配置
3. **Keyed State**：维护历史统计数据
4. **Side Output**：分离高风险信号
5. **Sliding Window**：多时间维度聚合
6. **Watermark**：处理乱序数据
7. **Rich Function**：访问外部系统

通过学习这个案例，可以掌握Flink在实时金融场景中的应用，理解如何构建一个生产级的实时分析系统。

## 相关文档

- [202603241545-ADS作业-实时市场监控指标计算.md](./202603241545-ADS作业-实时市场监控指标计算.md)
- [202603241630-ADS作业-跨市场套利机会计算.md](./202603241630-ADS作业-跨市场套利机会计算.md)
- [202603241745-Redis封装-创建连接管理器统一Redis操作.md](./202603241745-Redis封装-创建连接管理器统一Redis操作.md)
