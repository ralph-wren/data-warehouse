# 数据采集 - 添加杠杆过滤配置

## 问题描述

用户需要在 `FlinkTickerCollectorJob` 中添加配置选项，可以过滤掉不支持现货杠杆的币对，默认启用过滤。

## 解决方案

### 1. 配置文件修改

在 `application-dev.yml` 中添加杠杆过滤配置：

```yaml
ticker:
  subscribe:
    number: 20
  filter:
    margin-only: true  # 是否只订阅支持现货杠杆的币对(默认true,过滤掉不支持杠杆的币对)
```

### 2. 代码修改

#### 2.1 OKXRestApiSource 类修改

在 `OKXRestApiSource` 类中添加杠杆过滤功能：

**添加字段：**
```java
private final boolean filterMarginOnly;  // 是否只订阅支持现货杠杆的币对
private transient com.crypto.dw.trading.MarginSupportCache marginCache;  // 杠杆支持信息缓存
```

**构造函数修改：**
```java
public OKXRestApiSource(ConfigLoader config, long intervalMs, int topN) {
    this.config = config;
    this.intervalMs = intervalMs;
    this.topN = topN;
    this.filterMarginOnly = config.getBoolean("ticker.filter.margin-only", true);  // 默认过滤
}
```

**open() 方法修改：**
```java
@Override
public void open(org.apache.flink.configuration.Configuration parameters) throws Exception {
    super.open(parameters);
    objectMapper = new ObjectMapper();
    
    // 配置代理(如果启用)
    // ...
    
    // 初始化杠杆支持信息缓存（如果启用过滤）
    if (filterMarginOnly) {
        marginCache = new com.crypto.dw.trading.MarginSupportCache(config);
        logger.info("杠杆过滤已启用，将只订阅支持现货杠杆的币对");
    } else {
        logger.info("杠杆过滤已禁用，将订阅所有币对");
    }
}
```

**calculateSpreads() 方法修改：**
```java
public List<PriceSpreadInfo> calculateSpreads(
        Map<String, BigDecimal> spotPrices, 
        Map<String, BigDecimal> swapPrices) {
    
    List<PriceSpreadInfo> spreadList = new ArrayList<>();
    int filteredCount = 0;  // 被过滤的币对数量
    
    // 遍历所有现货价格
    for (Map.Entry<String, BigDecimal> entry : spotPrices.entrySet()) {
        String symbol = entry.getKey();
        BigDecimal spotPrice = entry.getValue();
        
        // 检查是否有对应的合约价格
        if (!swapPrices.containsKey(symbol)) {
            continue;
        }
        
        // 如果启用杠杆过滤，检查是否支持现货杠杆
        if (filterMarginOnly && marginCache != null) {
            String instId = symbol + "-USDT";
            boolean supportsMargin = marginCache.supportsMargin(instId);
            if (!supportsMargin) {
                filteredCount++;
                logger.debug("过滤不支持杠杆的币对: {}", instId);
                continue;
            }
        }
        
        // 计算价差...
        // ...
    }
    
    if (filterMarginOnly && filteredCount > 0) {
        logger.info("杠杆过滤: 过滤了 {} 个不支持杠杆的币对，剩余 {} 个币对", filteredCount, spreadList.size());
    }
    
    // 按价差率绝对值降序排序
    spreadList.sort((a, b) -> b.spreadRate.abs().compareTo(a.spreadRate.abs()));
    
    return spreadList;
}
```

## 工作流程

1. **启动时读取配置**：从配置文件读取 `ticker.filter.margin-only` 配置（默认 true）
2. **初始化杠杆缓存**：如果启用过滤，初始化 `MarginSupportCache`
3. **计算价差时过滤**：
   - 遍历所有现货价格
   - 检查是否有对应的合约价格
   - 如果启用杠杆过滤，检查币对是否支持现货杠杆
   - 过滤掉不支持杠杆的币对
   - 记录过滤数量
4. **输出日志**：记录过滤了多少个币对，剩余多少个币对

## 配置说明

- `ticker.filter.margin-only: true`：只订阅支持现货杠杆的币对（默认）
- `ticker.filter.margin-only: false`：订阅所有币对（不过滤）

## 优势

1. **减少无效订阅**：过滤掉不支持杠杆的币对，避免订阅无法交易的币对
2. **提高效率**：减少数据处理量，提高系统性能
3. **灵活配置**：可以通过配置文件灵活控制是否启用过滤
4. **详细日志**：记录过滤数量，方便监控和调试

## 编译和打包

```bash
mvn clean package -DskipTests
```

编译成功，生成 JAR 包：`target/realtime-crypto-datawarehouse-1.0.0.jar`

## 相关文件

- `src/main/resources/config/application-dev.yml`：配置文件
- `src/main/java/com/crypto/dw/flink/source/OKXRestApiSource.java`：数据源类
- `src/main/java/com/crypto/dw/trading/MarginSupportCache.java`：杠杆支持信息缓存类

## 日期

2026-04-11 22:00
