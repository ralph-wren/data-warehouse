# 套利作业 - 添加策略支持检查逻辑

**日期**: 2026-04-12 13:48  
**功能**: 套利作业开仓前策略支持检查  
**问题**: 开仓前未检查币种是否支持对应的套利策略，导致不支持杠杆的币种尝试策略A（现货卖出+合约买入）失败

---

## 问题背景

在套利作业 `FlinkADSArbitrageJob` 中，开仓逻辑未检查币种是否支持对应的套利策略：

- **策略A**：现货卖出 + 合约买入（需要杠杆借币）
- **策略B**：现货买入 + 合约卖出（不需要杠杆）

如果币种不支持杠杆借币，但尝试执行策略A，会导致下单失败，浪费资源。

---

## 解决方案

### 1. 修改 `TradingDecisionProcessor.java`

在 `openPosition()` 方法中添加策略支持检查逻辑：

```java
// ⭐ 第一步：检查策略支持（在检查持仓数量之前）
// 构造标准交易对格式（如 "BTC-USDT"）
String standardSymbol = opp.symbol + "-USDT";

// 判断套利方向对应的策略
boolean isStrategyA = opp.arbitrageDirection.contains("做空现货");  // 策略A：现货卖出 + 合约买入（需要杠杆）
boolean isStrategyB = opp.arbitrageDirection.contains("做多现货");  // 策略B：现货买入 + 合约卖出（不需要杠杆）

// 检查策略支持
if (isStrategyA && !marginCache.supportsStrategyA(standardSymbol)) {
    logger.warn("⚠️ 币种 {} 不支持策略A（现货卖出+合约买入，需要杠杆借币），拒绝开仓", opp.symbol);
    
    // 写入拒绝日志
    String timestamp = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
    String rejectLog = String.format("%s,%s,OPEN,REJECTED,N/A,N/A,N/A,N/A,REJECTED,不支持策略A（需要杠杆借币）",
        timestamp, opp.symbol);
    orderDetailWriter.write(rejectLog);
    return;
}

if (isStrategyB && !marginCache.supportsStrategyB(standardSymbol)) {
    logger.warn("⚠️ 币种 {} 不支持策略B（现货买入+合约卖出），拒绝开仓", opp.symbol);
    
    // 写入拒绝日志
    String timestamp = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
    String rejectLog = String.format("%s,%s,OPEN,REJECTED,N/A,N/A,N/A,N/A,REJECTED,不支持策略B",
        timestamp, opp.symbol);
    orderDetailWriter.write(rejectLog);
    return;
}

// 记录策略信息
if (isStrategyA) {
    int leverage = marginCache.getLeverage(standardSymbol);
    logger.info("✅ 策略A检查通过: {} | 杠杆倍数: {}x", opp.symbol, leverage);
} else if (isStrategyB) {
    logger.info("✅ 策略B检查通过: {} | 不需要杠杆", opp.symbol);
}
```

### 2. 检查顺序

开仓前的检查顺序调整为：

1. **策略支持检查**（新增）
   - 检查币种是否支持对应的套利策略
   - 策略A需要杠杆借币支持
   - 策略B不需要杠杆，所有币种都支持

2. **全局持仓数量检查**
   - 检查是否达到最大持仓数量限制

3. **下单执行**
   - 计算币数量
   - 执行现货和合约下单

---

## 策略说明

### 策略A：现货卖出 + 合约买入

- **操作**：卖出现货（做空） + 买入合约（做多）
- **要求**：需要杠杆借币支持
- **判断**：`marginCache.supportsStrategyA(symbol)` 返回 `true`
- **Redis 数据**：`okx:margin:support` 中 value > 0（如 "10" 表示 10 倍杠杆）

### 策略B：现货买入 + 合约卖出

- **操作**：买入现货（做多） + 卖出合约（做空）
- **要求**：不需要杠杆
- **判断**：`marginCache.supportsStrategyB(symbol)` 返回 `true`
- **Redis 数据**：只要在 `okx:margin:support` 中存在即可

---

## 相关文件

- `src/main/java/com/crypto/dw/processor/TradingDecisionProcessor.java` - 添加策略支持检查
- `src/main/java/com/crypto/dw/trading/MarginSupportCache.java` - 提供策略支持查询方法
- `src/main/java/com/crypto/dw/flink/FlinkTickerCollectorJob.java` - 写入杠杆倍数到 Redis
- `src/main/java/com/crypto/dw/trading/OKXTradingService.java` - 获取杠杆倍数

---

## 测试建议

1. 启动 `FlinkTickerCollectorJob` 采集杠杆支持信息
2. 检查 Redis `okx:margin:support` 中的数据格式
3. 启动 `FlinkADSArbitrageJob` 套利作业
4. 观察日志中的策略检查信息：
   - `✅ 策略A检查通过: BTC | 杠杆倍数: 10x`
   - `✅ 策略B检查通过: ETH | 不需要杠杆`
   - `⚠️ 币种 XXX 不支持策略A（现货卖出+合约买入，需要杠杆借币），拒绝开仓`

---

## 优化效果

1. **避免无效下单**：不支持杠杆的币种不会尝试策略A
2. **提高成功率**：只对支持的策略进行开仓
3. **日志清晰**：记录拒绝原因，方便排查问题
4. **资源节省**：减少无效的 API 调用和错误处理
